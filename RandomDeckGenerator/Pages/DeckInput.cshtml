@page
@using System.Reflection.Metadata
@model RandomDeckGenerator.Pages.DeckInput

@{
    var input = Model.inputs;
}

<div>
    <form method="post">
        <table>
                <thead></thead>
                <thead></thead>
                <thead></thead>
                <thead></thead>
                @{
                    var index = 0;
                    for (var i = 0; i < 13; i++)
                    {
                        <tr>
                            <td><input type="text" name="dataToInput" value=@input[index]></td>
                            @{ index++;}
                            <td><input type="text" name="dataToInput" value=@input[index]></td>
                            @{ index++;}
                            <td><input type="text" name="dataToInput" value=@input[index]></td>
                            @{ index++;}
                            <td><input type="text" name="dataToInput" value=@input[index]></td>
                            @{ index++;}
                        </tr>
                    }
                    index = 0;
                }
            </table>
        @{
            <input type="Submit"/>
        }
    </form>
</div>